TARGETS=<target>.com  									#target you want to run the framework against
cewltmp=/data/cewl.txt 								#wordlist generated by cewl gets saved here
cewlLOC=/opt/subscan/wordlists/cewlwordlist.txt 	#location inside the docker container where the cewl wordlist is saved 
LOC=/opt/subscan/wordlists							#location where all wordlists exists inside the container
finalLOC=/data/subnames.txt							#final location of all wordlists after merging (cewl+others)

temp1=/data/output/temp1.txt						#temp1 file
temp2=/data/output/temp2.txt						#temp2 file
temp3=/data/output/temp3.txt						#temp3 file
temp4=/data/output/temp4.txt						#temp4 file
gobusterfile=/data/output/gobusteroutput.txt		#gobuster output gets saved here
enumallfile=/data/output/enumalloutput.txt			#enumall output gets saved here
sublist3rfile=/data/output/sublist3routput.txt		#sublist3r output gets saved here

google_api=<ADD>									#ADD YOUR KEY
google_cse=<ADD>									#ADD YOUR KEY	
shodan_api=<ADD>									#ADD YOUR KEY

resolveserver=8.8.8.8:53							#DNS server to resolve
altdnsserver=8.8.8.8								#DNS server for altdns to resolve
	
finaloutput=/data/finaloutput.txt					#final output of bruteforced subdomains before running altdns

altdnsoutput=/data/altdnsoutput.txt					#altdns output which contains subdomains and resolved IPs/domains
altdnsonlysubs=/data/altdnsonlysubs.txt				#altdns output sorted to only have the subdomains
finalandaltdns=/data/finalandaltdns.txt				#final final output combining altdns output with output from other tools